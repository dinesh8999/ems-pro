PORT=5000
MONGODB_URI=mongodb://localhost:27017/ems
JWT_SECRET=your_jwt_secret_key_here_change_in_production
NODE_ENV=development
# Optional: comma-separated origins for production CORS, e.g. https://app.example.com
CORS_ORIGIN=
# Optional: rate limit max requests per 15 minutes
RATE_LIMIT_MAX=100
